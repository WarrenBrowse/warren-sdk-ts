packages:
  - "packages/*"
  - "examples/*"

# tsup still asks for esbuild 0.27, whose development server has a file-read
# advisory; the build uses only the bundler API, which 0.28 keeps.
overrides:
  esbuild: ^0.28.1
